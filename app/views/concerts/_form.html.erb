<%= form_with(model: concert, local: true) do |form| %>
  <% if concert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
      <% concert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :attendance %>
    <%= form.number_field :attendance, id: :concert_attendance %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.number_field :duration, id: :concert_duration %>
  </div>

  <div class="field">
    <%= form.label :concert_date %>
    <%= form.date_select :concert_date, id: :concert_concert_date %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.collection_select :group_id, @groups, :id, :name%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
